export default function TestPage() {
  return (
    <div style={{ padding: 40, color: 'white', background: '#111', minHeight: '100vh' }}>
      <h1 style={{ fontSize: 32 }}>FitStake Test Page</h1>
      <p>If you can see this, Next.js is working correctly.</p>
      <p>No providers, no wagmi, no Privy — just plain HTML.</p>
    </div>
  )
}
